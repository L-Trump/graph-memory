{
  "name": "graph-memory",
  "version": "3.0.0",
  "description": "Knowledge Graph Memory Engine for OpenClaw — precise recall with Personalized PageRank, access decay, vector dedup, and retention maintenance",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "tsup",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@photostructure/sqlite": "^1.0.0",
    "@sinclair/typebox": "^0.34.48",
    "openai": "^4.47.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsup": "^8.5.1",
    "typescript": "^5.9.3",
    "vitest": "^1.4.0"
  },
  "peerDependencies": {
    "openclaw": "*"
  },
  "openclaw": {
    "extensions": [
      "./dist/index.js"
    ]
  }
}
